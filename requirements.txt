codeact @ git+https://github.com/zzszmyf/codeact
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Multi-datasource drivers
sqlalchemy>=2.0
pymysql>=1.1
psycopg2-binary>=2.9
pymssql>=2.2
elasticsearch>=8.0,<9.0
pyhive[hive]>=0.7
impyla>=0.19
pymilvus>=2.3

# Vector databases
pgvector>=0.3
qdrant-client>=1.9
weaviate-client>=4.6
chromadb>=0.5

# Heavy drivers — see docs/数据源.md for external client requirements:
#   oracledb (python-oracledb) -> thin 模式无需客户端；thick 模式需 Oracle Instant Client
#   ibm_db                      -> 需 IBM DB2 clidriver (libdb2)
oracledb>=2.0
ibm_db>=3.2
